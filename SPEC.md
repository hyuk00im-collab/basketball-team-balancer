# 농구 동호회 팀 편성 프로그램 — 데이터 스펙 (SPEC.md)

이 문서는 `basketball_level_sheet.xlsx` 형식의 인원 데이터를 읽어
게스트를 포함한 팀을 자동으로 편성하는 프로그램을 만들기 위한
데이터 스펙입니다. Claude Code 프로젝트 루트에 이 파일을 두고
개발 시 참조 문서로 사용합니다.

## 1. 입력 파일 개요

- 파일 형식: `.xlsx`
- 시트명: `선수레벨` (데이터), `작성안내` (사람이 보는 설명, 파싱 대상 아님)
- 데이터 헤더 행: 3행 (컬럼명)
- 예시행: 4행 (샘플 데이터, **파싱 시 제외**)
- 실제 데이터: 5행부터 시작, 빈 이름(A열) 행이 나오면 데이터 끝으로 간주

## 2. 컬럼 매핑 (열 순서 고정)

| 열 | 컬럼명 | 타입 | 필수 | 비고 |
|---|---|---|---|---|
| A | 이름 | string | 필수 | 동명이인 가능 → 내부적으로 row index를 고유 id로 사용 |
| B | 출생년도 | int | 선택 | 1930~현재 연도. **나이 대신 이 값을 입력** |
| C | 나이 | int (수식) | 자동계산 | `=IF(B5="","",YEAR(TODAY())-B5)` — 파일을 여는 시점의 연도 기준 |
| D | 키(cm) | int | 선택 | 140~230 |
| E | 포지션 | string | 필수 | `가드` 또는 `포워드` |
| F~O | 가드 역량 10항목 | float | 포지션=가드일 때만 값 존재 | 0~5, 0.5 단위 |
| P~Y | 포워드 역량 10항목 | float | 포지션=포워드일 때만 값 존재 | 0~5, 0.5 단위 |
| Z | 역량평균 | float (수식) | 자동계산 | 포지션에 따라 F~O 또는 P~Y 평균 |

### 포지션에 따른 역량 블록 분기

템플릿 파일(`GET /api/sample`)은 포지션(E열)에 따라 역량 블록이 갈리도록 만들어져 있습니다.

- **역량평균(Z열)**: 포지션이 `가드`면 F~O, `포워드`면 P~Y만 평균냅니다.
  ```
  =IF($A5="","",IF($E5="가드",IF(COUNT(F5:O5)=0,"",AVERAGE(F5:O5)),
                 IF($E5="포워드",IF(COUNT(P5:Y5)=0,"",AVERAGE(P5:Y5)),"")))
  ```
- **조건부 서식**: 해당 포지션이 아닌 블록은 회색으로 가려집니다.
  포지션이 비어 있으면 양쪽 모두 가려집니다.
- **데이터 유효성 검사**: 포지션은 드롭다운(`가드`/`포워드`), 역량은 해당 포지션 블록에만
  0~5(0.5 단위) 입력이 허용됩니다. 반대쪽 블록에 입력하면 거부됩니다.

파서는 이와 무관하게 포지션에 해당하는 블록만 읽으므로, 잘못된 블록에 값이 남아 있어도
결과에 영향을 주지 않습니다.

### 나이 자동 계산

- 나이(C열)는 직접 입력하지 않고 출생년도(B열)에서 수식으로 산출합니다.
  ```
  =IF(B5="","",YEAR(TODAY())-B5)
  ```
- 행을 추가할 때는 위 행의 C열·Z열 수식을 아래로 복사합니다.
- 파서는 openpyxl로 값을 읽으므로 수식의 캐시값이 없을 수 있습니다. 따라서
  **출생년도가 있으면 나이 셀을 무시하고 서버의 현재 연도로 다시 계산**합니다.
  (`YEAR(TODAY())` 갱신 시점과 무관하게 항상 최신 나이가 됩니다.)
- 파서는 3행 헤더의 **컬럼명으로 열을 찾으므로**, 출생년도 열이 없던 이전 형식의
  파일도 그대로 읽힙니다(이 경우 나이 열 값을 그대로 사용).

### 가드 역량 (F~O, 10개)
1. 볼핸들링·드리블
2. 코트비전·패스센스
3. 3점슈팅
4. 미드레인지 풀업슈팅
5. 퍼스트스텝·돌파력
6. 속공전개능력
7. 온볼수비(맨투맨)
8. 스틸·패스인터셉트
9. 경기운영·템포조절
10. 자유투능력

### 포워드 역량 (P~Y, 10개)
1. 리바운드
2. 포스트업득점력
3. 인사이드피니시
4. 수비능력
5. 박스아웃·몸싸움
6. 스크린능력
7. 자유투
8. 미드레인지 슈팅
9. 패싱센스
10. 블록슛

## 3. 파생 데이터 정의

프로그램은 파싱 후 각 인원을 아래 구조의 객체로 변환합니다.

```json
{
  "id": "행 번호 기반 고유값 (예: row_5)",
  "name": "홍길동",
  "birth_year": 1993,
  "age": 32,
  "height_cm": 178,
  "position": "가드",
  "skills": {
    "볼핸들링·드리블": 4.0,
    "코트비전·패스센스": 4.5,
    "...": "..."
  },
  "level": 3.65,
  "is_guest": false
}
```

- `level` = Z열 값(역량평균)을 그대로 사용 (수식 캐시값이 없으면 10항목 평균으로 계산)
- `age` = 출생년도가 있으면 `현재 연도 - 출생년도`, 없으면 나이 열 값
- `is_guest`: 원본 파일 인원은 `false`, 프로그램 실행 시 추가 입력된 인원은 `true`
- `skills` 딕셔너리는 포지션에 해당하는 10개 항목만 채움 (없는 포지션 항목은 포함하지 않음)

## 4. 게스트 추가 입력 스펙

게스트는 원본 엑셀 파일 구조와 동일한 필드로 입력받습니다 (엑셀 파일 업로드 또는 CLI/폼 입력 중 프로그램 구현 방식에 따라 선택):

- 필수 입력: 이름, 포지션
- 선택 입력: 출생년도(입력 시 나이 자동 계산), 나이, 키
- 역량 10항목: 미입력 시 기본값 처리 방식 결정 필요 (예: 해당 포지션 전체 인원 평균값으로 대체, 또는 임의값 3.0 고정 — **팀 편성 로직 구현 전에 정책 확정 필요**)

## 5. 팀 편성 로직 요구사항 (구현 시 정의할 파라미터)

아래 항목은 프로그램 구현 단계에서 구체적인 알고리즘으로 확정해야 하는 요구사항입니다.

- **팀 수 / 팀당 인원수**: 입력값으로 받음 (예: 2팀, 팀당 5명)
- **포지션 밸런스**: 각 팀에 가드/포워드 비율을 균등하게 배분 (예: 5인 1팀 기준 가드 2~3명, 포워드 2~3명)
- **레벨(역량평균) 밸런스**: 팀별 `level` 합계 또는 평균이 팀 간 최대한 균등하도록 배정
- **키(height_cm) 밸런스**: 선택적으로 팀별 평균 신장 편차 최소화 (옵션)
- **고정 배정/제외 인원**: 특정 인원을 특정 팀에 고정하거나 팀 편성에서 제외하는 기능 필요 여부 확인
- **랜덤 시드**: 동일 조건에서 재현 가능한 결과가 필요한지 여부 (필요 시 seed 고정 옵션)

## 6. 출력 스펙

- 팀별 명단 (이름, 포지션, 레벨 표시)
- 팀별 요약: 평균 레벨, 가드/포워드 인원수, 평균 키
- 출력 형식: 콘솔 텍스트 / xlsx 재출력 / html 중 택 1 이상 (구현 시 결정)

## 7. 향후 변경 시 주의사항

- 원본 엑셀의 **열 순서와 헤더명은 이 스펙과 반드시 일치**해야 프로그램이 정상 파싱됨
- 역량 항목명이나 개수를 변경할 경우 이 문서와 파서 코드를 함께 수정
- `작성안내` 시트는 파싱 대상에서 제외
