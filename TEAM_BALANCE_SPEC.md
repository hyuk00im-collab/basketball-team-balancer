# 농구 동호회 팀 밸런싱 — 전문 스펙 정의서 (TEAM_BALANCE_SPEC.md)

이 문서는 `SPEC.md`(데이터 스키마)를 기반으로, 실제 팀을 어떻게
"공정하게" 나눌 것인지에 대한 이론적 근거와 알고리즘 설계 기준을
정의한다. 아마추어 동호회 픽업/리그 경기에서 팀 밸런싱은 단순히
개인 역량 점수를 합산하는 문제가 아니라, **포지션 구성, 역량의
질적 성격(득점형 vs 지원형), 신체 조건, 팀 내 상호보완성**을
함께 고려해야 하는 다변수 최적화 문제다. 이 문서는 그 기준을
정리하여 이후 Claude Code로 구현할 편성 알고리즘의 근거로 삼는다.

---

## 1. 팀 밸런싱의 4가지 축

농구 팀 구성의 공정성은 단일 지표가 아니라 아래 4가지 축의
합성으로 판단해야 한다.

| 축 | 정의 | 이 프로젝트에서의 대응 데이터 |
|---|---|---|
| ① 개인 역량 총량 (Talent Volume) | 팀 전체 역량 점수의 합/평균 | `level`(역량평균), 세부 `skills` |
| ② 포지션 구조 (Positional Structure) | 가드/포워드 비율과 롤 분포 | `position` |
| ③ 신체 조건 (Physical Profile) | 평균 신장, 신장 편차 | `height_cm` |
| ④ 역할 상호보완성 (Role Complementarity) | 득점형/수비형/연결형 역할이 한쪽에 몰리지 않도록 | `skills` 세부 항목 프로파일 |

단순 평균 점수만 맞추면 ①만 만족시키고 ②~④가 무너질 수 있다.
예: 레벨 합산은 같아도 한 팀에 3점슈터만 몰리고 다른 팀에
수비/리바운드형만 몰리면 실제 경기 밸런스는 깨진다.

---

## 2. 역량 항목의 재분류 (Skill Taxonomy)

기존에 정의한 가드/포워드 각 10항목을 팀 밸런싱 알고리즘 관점에서
아래 4개 역할군(Role Cluster)으로 재분류한다. 이는 원본 항목을
대체하는 것이 아니라, 같은 항목을 알고리즘이 다른 관점에서
가중치를 부여할 수 있도록 태깅하는 메타데이터다.

### 2.1 가드 항목의 역할군 매핑

| 역할군 | 해당 항목 |
|---|---|
| 득점(Scoring) | 3점슈팅, 미드레인지 풀업슈팅, 자유투능력 |
| 창출(Creation) | 볼핸들링·드리블, 코트비전·패스센스, 경기운영·템포조절 |
| 스피드/전환(Tempo) | 퍼스트스텝·돌파력, 속공전개능력 |
| 수비(Defense) | 온볼수비(맨투맨), 스틸·패스인터셉트 |

### 2.2 포워드 항목의 역할군 매핑

| 역할군 | 해당 항목 |
|---|---|
| 득점(Scoring) | 포스트업득점력, 인사이드피니시, 미드레인지 슈팅, 자유투 |
| 창출(Creation) | 패싱센스, 스크린능력 |
| 골밑장악(Interior Control) | 리바운드, 박스아웃·몸싸움, 블록슛 |
| 수비(Defense) | 수비능력 |

### 2.3 활용 원칙

- 한 팀에 "득점(Scoring)" 역할군 점수가 높은 인원만 몰리지 않도록
  팀별 역할군 평균 점수의 팀 간 분산을 최소화한다.
- "창출(Creation)" 역할(패스/스크린/경기운영)이 0에 가까운 팀이
  생기지 않도록 팀당 최소 1인 이상 창출형 인원 배치를 권장한다.
- 이는 실전 농구에서 "볼 배급자 부재로 인한 공격 정체"를
  방지하기 위한 경험적 기준이다.

---

## 3. 종합 레벨 산출 방식 (Composite Level Score)

현재 `SPEC.md`의 `level`은 포지션별 10항목의 단순 평균이다.
전문 스펙에서는 아래와 같이 두 단계로 정교화할 것을 권장한다.

### 3.1 1단계 — 원 데이터 유지
`level_raw` = 기존 단순 평균 (하위 호환용, `SPEC.md` Y열 그대로)

### 3.2 2단계 — 역할군 균형 가중 평균 (선택적 고도화)
```
level_weighted = (득점_avg * w1) + (창출_avg * w2)
               + (수비_avg * w3) + (골밑장악 or 스피드/전환_avg * w4)
```
- 초기값 제안: w1=0.3, w2=0.25, w3=0.25, w4=0.2 (균등에 가까운 배분)
- 가중치는 동호회 성격(득점 중심 vs 수비 중심 리그)에 따라
  조정 가능한 파라미터로 둔다.
- **알고리즘 설계상 원칙**: `level_weighted`는 어디까지나 보조
  지표이며, 실제 편성은 §4의 다목적 최적화에서 `level_raw`와
  역할군별 점수를 별도 제약조건으로 함께 사용하는 것을 권장한다
  (단일 스칼라로 뭉치면 정보 손실이 크기 때문).

---

## 4. 팀 편성 알고리즘 설계 원칙

### 4.0 실질 전력(effective level) — 나이 보너스 반영

경기 규칙상 **55세 이상 인원은 가중치 0.5점**을 받는다. 같은 역량이라도 팀에
기여하는 실제 값이 그만큼 크므로, 밸런싱은 순수 역량이 아니라 아래 값으로 한다.

```
effective_level = level_raw + age_bonus
age_bonus = 0.5  (나이 >= 55), 그 외 0
```

- 목적함수의 전력 편차, 팀 요약의 평균/합계, 평가 지표는 모두 `effective_level`
  기준이다. `level_raw`는 순수 역량으로 함께 표시한다.
- 나이 미상 인원은 보너스 0으로 본다.
- 기준 나이와 보너스 점수는 파라미터(`age_bonus_from`, `age_bonus`)로 조정할 수 있다.

### 4.1 문제 정의
N명(원 인원 + 게스트)을 K개 팀, 팀당 M명으로 분할하되 아래
목적함수를 최소화하는 **다목적 균형 분할 문제(Multi-objective
Balanced Partitioning)**로 정의한다.

```
Minimize:
  Var(team_effective_level)    # 팀 간 실질 전력 편차 (나이 보너스 포함)
  + λ1 * Var(team_position_ratio)   # 가드/포워드 비율 편차
  + λ2 * Var(team_role_cluster_avg) # 역할군별 평균 편차 (4개 역할군 각각)
  + λ3 * Var(team_height_avg)       # 평균 신장 편차 (옵션)
  + P1 * 포지션 쿼터 위반 팀 수      # §4.3
  + P2 * 창출형 0명 팀 수            # §4.4
  + P3 * 역량 상위권 분산 위반 수    # §4.5 1순위 (P3=10, 사실상 하드 제약)
  + P4 * 신장 상위권 분산 위반 수    # §4.5 2순위 (P4=5, 충돌 시 이쪽을 양보)
```

### 4.2 권장 알고리즘 (구현 난이도순)

| 난이도 | 알고리즘 | 설명 | 권장 상황 |
|---|---|---|---|
| 하 | 스네이크 드래프트(Snake Draft) | `level_raw` 내림차순 정렬 후 1-2-2-1 순으로 배분 | 빠른 구현, 인원 20명 이하 |
| 중 | 그리디 최소편차 배정(Greedy Balanced Assignment) | 정렬된 인원을 "현재 가장 총점 낮은 팀"에 순차 배정 + 포지션 쿼터 제약 | 기본 권장안 |
| 상 | 지역 탐색/스왑 최적화(Local Search / Simulated Annealing) | 그리디 결과에서 팀 간 인원 스왑을 반복하며 §4.1 목적함수 개선 | 정밀도 필요, 인원 많을 때 |

**권장 구현 순서**: 먼저 그리디 배정으로 초기해를 만들고,
이후 일정 반복 횟수(예: 1,000회) 동안 무작위 2인 스왑을 시도해
목적함수가 개선될 때만 채택하는 방식(Hill Climbing)을 1차
구현 목표로 한다. 스네이크 드래프트는 베이스라인 비교용으로만
사용한다.

### 4.3 포지션 쿼터 제약
- 팀당 가드:포워드 비율은 입력 인원 전체 비율에서 팀별 편차
  ±1명 이내로 제한한다.
- 5인 기준 시 일반적으로 가드 2~3 : 포워드 2~3 구성을 기본값으로
  하되, 전체 인원의 실제 가드/포워드 비율이 크게 기울어 있으면
  (예: 가드 절대 부족) 이 제약을 완화하는 fallback 로직이 필요하다.

### 4.4 창출형 인원 최소 보장 제약
- §2.3에서 정의한 "창출(Creation)" 역할군 점수 상위 N% 인원이
  특정 팀에 0명 배정되지 않도록 하는 제약을 추가한다.
- 근거: 픽업 농구에서 볼 운반/분배가 전혀 안 되는 팀은 실제
  체감 전력차가 스탯 평균 차이보다 크게 벌어지는 경향이 있다
  (경기 흐름 단절 → 공격 기회 손실).

### 4.5 상위권 분산 제약 (우선순위가 있는 하드 제약)

두 개의 분산 제약을 **우선순위를 두고** 적용한다.

| 순위 | 제약 | 대상 | 페널티 |
|---|---|---|---|
| 1순위 | **역량** 순위 1·2·3위를 서로 다른 팀에 | 가드, 포워드 각각 | P3 = 10.0 |
| 2순위 | **신장** 순위 1·2·3위를 서로 다른 팀에 | 포워드 | P4 = 5.0 |

- 순위는 모두 **순수 값 기준**이다. 나이 보너스는 순위에 영향을 주지 않는다
  (보너스는 전력 계산에만 반영). 동점자는 이름순으로 순위를 확정해 같은 입력에서
  항상 같은 결과가 나오게 한다. 키가 입력되지 않은 인원은 신장 순위에서 제외한다.
- 근거: 픽업 농구에서 팀 전력은 평균보다 **최상위 인원의 편중**에 훨씬 민감하다.
  총점을 맞춰도 에이스 2명이 한 팀에 몰리면 체감 전력차가 크게 벌어진다.
  골밑 장악력도 마찬가지로, 장신 포워드가 한 팀에 몰리면 리바운드 싸움이 일방적이 된다.
- 구현: 그리디 초기해에서 **1순위 그룹 → 2순위 그룹 순서로** 서로 다른 팀에 하나씩
  배치하고, 이후 스왑 최적화에서는 목적함수의 큰 페널티로 위반을 차단한다.
  같은 그룹끼리 팀을 맞바꾸는 스왑은 제약을 유지하므로 허용된다.
- **충돌 시 처리**: 두 제약을 동시에 만족할 수 없으면 페널티가 작은 2순위(신장)를
  양보한다. P4 < P3 라는 크기 관계가 곧 우선순위다.
- 예외 처리: 팀 수보다 상위권이 많거나(K<3), 고정 배정(locked)으로 상위권이
  같은 팀에 묶인 경우에는 제약을 완화하고 위반 건수를 리포트에 표시한다.

---

## 5. 밸런스 평가 지표 (Post-hoc Evaluation)

편성 완료 후 아래 지표로 결과를 정량 평가하고 리포트에 포함한다.

| 지표 | 산출식 | 목표 |
|---|---|---|
| 레벨 편차율 | (팀별 effective_level 합 최대값 - 최소값) / 전체 평균 | 5% 이내 권장 (팀 인원수가 같을 때만 유효) |
| 레벨 편차율(평균) | (팀별 effective_level 평균 최대-최소) / 전체 평균 | 5% 이내 — 인원수가 다를 때의 실질 지표 |
| 역량 상위권 분산 위반 | §4.5 1순위 제약을 어긴 건수 | 0 |
| 신장 상위권 분산 위반 | §4.5 2순위 제약을 어긴 건수 | 0 (1순위와 충돌 시 양보) |
| 포지션 쿼터 위반 수 | 쿼터 제약을 벗어난 팀 수 | 0 |
| 역할군 균형 지수 | 4개 역할군 평균의 팀 간 표준편차 합 | 낮을수록 우수 |
| 신장 편차 | 팀별 평균 신장 최대-최소 차 | 참고용 (강제 아님) |

---

## 6. 게스트 및 결측치 처리 정책 (확정안)

이전 문서(§4, `SPEC.md`)에서 미정으로 남겨두었던 부분을
전문가 관점에서 아래와 같이 확정 제안한다.

- 게스트가 세부 역량을 입력하지 않은 경우, **동일 포지션 기존
  회원들의 항목별 중앙값(median)**으로 대체한다. 평균보다
  중앙값이 이상치(매우 잘하거나 못하는 소수 인원)에 덜 민감하여
  더 안전한 기본값이다.
- 게스트의 `level_raw`는 대체된 항목 값으로 재계산하며, UI/출력
  시에는 "추정값(estimated)"임을 별도 표기한다.

---

## 7. 향후 확장 여지 (Out of Scope, 참고용)

아래 항목은 이번 버전 구현 범위에는 포함하지 않되, 데이터가
누적되면 고도화할 수 있는 방향으로 기록해 둔다.

- **실제 경기 결과 피드백 루프**: 편성된 팀의 승패 데이터를
  누적하여 `level_raw`를 보정하는 ELO/Glicko류 레이팅 시스템 도입
- **케미스트리(호흡) 데이터**: 특정 인원 간 함께 뛴 경기의
  승률 데이터를 반영한 페어링 가중치
- **포지션 세분화**: 가드/포워드 2분류를 포인트가드·슈팅가드·
  스몰포워드·파워포워드·센터 5분류로 확장할 경우 스펙 전면 개정 필요

---

## 8. 이 문서와 `SPEC.md`의 관계

- `SPEC.md`: 데이터 스키마(입력 파일 구조, 컬럼 정의) — **무엇을
  읽어올 것인가**
- `TEAM_BALANCE_SPEC.md`(본 문서): 편성 알고리즘의 이론적 근거와
  설계 기준 — **어떻게 나눌 것인가**

Claude Code 구현 시 두 문서를 함께 프로젝트 루트에 두고,
파서/데이터 모델은 `SPEC.md`를, 편성 로직·목적함수·제약조건은
본 문서를 근거로 작성한다.
