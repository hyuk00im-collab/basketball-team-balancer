# 농구 팀 편성 웹앱 — 프로젝트 요구사항 정의서 (WEBAPP_SPEC.md)

- 프로젝트 경로: `C:\AICoding2`
- 목적: 인원의 신상정보·역량을 입력받아 **3개 팀**으로 자동 편성해주는
  웹 애플리케이션
- 참조 문서: `SPEC.md`(데이터 스키마), `TEAM_BALANCE_SPEC.md`(편성 알고리즘 이론)
  — 이 세 파일을 프로젝트 루트에 함께 두고 Claude Code로 개발 진행

---

## 1. 기술 스택 (제안)

| 영역 | 선택 | 이유 |
|---|---|---|
| 백엔드 | Python + FastAPI (또는 Flask) | xlsx 파싱(openpyxl/pandas), 편성 알고리즘(§4 로직) 구현이 Python이 수월 |
| 프론트엔드 | HTML/CSS/JS (또는 React) — 우선 단순 HTML/JS로 MVP | 초기 버전은 입력폼 + 결과 테이블 정도로 단순, 과설계 지양 |
| 데이터 저장 | 세션 단위 메모리 처리 (MVP), 이후 필요 시 SQLite로 영속화 | 초기엔 파일 업로드→즉시 편성→결과 표시 흐름이면 충분 |
| 실행 방식 | 로컬 웹서버 (uvicorn 등)로 `localhost` 구동 | 동호회 내부용, 배포 불필요 |

> **가정**: 별도 스택 선호가 없다고 판단해 Python 백엔드로 제안했습니다.
> Node.js/React 등 다른 스택을 원하시면 이 표만 교체하면 나머지 스펙은 동일하게 적용됩니다.

---

## 2. 폴더 구조 (제안)

```
C:\AICoding2\
├── SPEC.md                    # 데이터 스키마
├── TEAM_BALANCE_SPEC.md        # 편성 알고리즘 이론
├── WEBAPP_SPEC.md              # 본 문서
├── backend\
│   ├── main.py                 # FastAPI 앱 진입점
│   ├── models.py                # 인원/팀 데이터 모델 (SPEC.md §3 기준)
│   ├── parser.py                # xlsx 업로드 파싱 (SPEC.md §1~2 기준)
│   ├── balancer.py              # 팀 편성 알고리즘 (TEAM_BALANCE_SPEC.md §4 기준)
│   └── evaluator.py             # 편성 결과 평가 지표 (TEAM_BALANCE_SPEC.md §5 기준)
├── frontend\
│   ├── index.html               # 인원 입력/업로드 화면
│   ├── result.html              # 3개 팀 결과 화면
│   └── static\ (css, js)
└── requirements.txt
```

---

## 3. 화면/기능 정의

### 3.1 입력 화면 (Input Page)

두 가지 입력 방식을 모두 지원한다.

**방식 A — 엑셀 업로드**
- `SPEC.md` 형식(`선수레벨` 시트, A~Y열)의 xlsx 파일 업로드
- 업로드 즉시 서버에서 파싱하여 인원 목록 미리보기 표시

**방식 B — 웹 폼 직접 입력 (개별 인원 추가/게스트 추가용)**
- 입력 필드: 이름(필수), 나이, 키(cm), 포지션(가드/포워드, 필수)
- 포지션 선택 시 해당 포지션의 역량 10항목 슬라이더/숫자 입력(0~5, 0.5 단위) 동적 표시
- "인원 추가" 버튼으로 목록에 계속 추가 가능 (게스트도 동일 방식)
- 역량 항목 미입력 시 `TEAM_BALANCE_SPEC.md` §6 정책(동일 포지션 중앙값 대체) 적용 안내 문구 표시

**공통**
- 현재까지 입력/업로드된 전체 인원 목록을 표(이름/포지션/레벨)로 실시간 표시
- 목록에서 개별 인원 삭제 기능
- 하단 "팀 편성하기" 버튼 (최소 인원 수 검증: 3팀 편성이므로 최소 3명 이상, 권장 인원수 안내)

### 3.2 팀 편성 실행

- "팀 편성하기" 클릭 시 백엔드에 전체 인원 데이터 전송
- `TEAM_BALANCE_SPEC.md` §4의 알고리즘(그리디 배정 → 스왑 최적화)으로 **3개 팀**으로 분할
  - 팀 수(K=3)는 고정, 팀당 인원수는 전체 인원을 3등분 (나머지는 라운드로빈 배분)
  - 포지션 쿼터, 창출형 인원 최소 보장 제약 적용
- 처리 중 로딩 인디케이터 표시 (인원 많을 시 스왑 최적화에 다소 시간 소요 가능)

### 3.3 결과 화면 (Result Page)

- 3개 팀을 카드 또는 컬럼 형태로 나란히 표시
- 팀별 표시 항목:
  - 팀 이름(기본값 "1팀/2팀/3팀", 수정 가능)
  - 소속 인원 목록: 이름, 포지션, 레벨(역량평균)
  - 팀 요약: 평균 레벨, 가드/포워드 인원수, 평균 키
- 상단에 `TEAM_BALANCE_SPEC.md` §5 평가지표(레벨 편차율, 포지션 쿼터 위반 수 등) 요약 표시
- "다시 편성" 버튼: 동일 인원으로 재계산(랜덤 시드 변경) 또는 수동으로 인원 팀 간 드래그 이동(선택 기능, MVP 이후)
- 결과를 이미지/텍스트로 복사하거나 xlsx로 재출력하는 기능(선택 기능, MVP 이후)

---

## 4. MVP 범위 정의

| 우선순위 | 기능 |
|---|---|
| P0 (필수) | 엑셀 업로드 파싱, 웹 폼 개별 입력, 3팀 그리디 배정, 결과 화면 표시 |
| P1 (권장) | 스왑 기반 최적화(Hill Climbing), 평가지표 요약 표시, 결측치 중앙값 대체 |
| P2 (선택) | 결과 xlsx 재출력, 수동 드래그 조정, 팀 이름 커스텀, 재편성(시드 변경) |

Claude Code 개발 시 P0부터 순차 구현을 권장한다.

---

## 5. API 설계 초안 (백엔드가 FastAPI일 경우)

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/upload` | xlsx 업로드 → 파싱된 인원 목록(JSON) 반환 |
| POST | `/api/members` | 웹 폼으로 인원 1명 추가 (JSON body) |
| GET | `/api/members` | 현재까지 등록된 전체 인원 목록 조회 |
| DELETE | `/api/members/{id}` | 인원 삭제 |
| POST | `/api/teams/generate` | 등록된 전체 인원으로 3팀 편성 실행 → 결과(JSON) 반환 |

각 엔드포인트의 요청/응답 데이터 형식은 `SPEC.md` §3의 인원 객체 구조를 그대로 따른다.

---

## 6. 이 문서와 나머지 문서의 관계

- `SPEC.md`: 데이터가 **무엇**인지 (스키마)
- `TEAM_BALANCE_SPEC.md`: **어떻게** 나눌지 (알고리즘 이론)
- `WEBAPP_SPEC.md`(본 문서): 그것을 **어떤 화면·API로 제공**할지 (제품 요구사항)

Claude Code에게 개발을 지시할 때는 "`WEBAPP_SPEC.md`, `SPEC.md`,
`TEAM_BALANCE_SPEC.md`를 참고해서 C:\AICoding2에 웹앱을 만들어줘"
형태로 세 문서를 함께 컨텍스트로 제공하는 것을 권장한다.
